---
import { homepage, type PracticeId } from '../data/homepage';
import SectionDotNav from './SectionDotNav.astro';

interface Props {
  mode?: 'home' | 'work';
}

const { mode = 'home' } = Astro.props as Props;
const isWorkPage = mode === 'work';

const practiceClass: Record<PracticeId, string> = {
  systems: 'systems',
  'embodied-life': 'embodied',
  'inhabited-worlds': 'worlds',
};

const materialStyle = ({ from, to }: { from: string; to: string }) =>
  `--m-from: ${from}; --m-to: ${to};`;

const dotNav = [
  { id: 'intro', label: 'Intro', accent: 'ink' },
  { id: 'topics', label: 'Topics', accent: 'muted' },
  ...homepage.workGroups.map((group) => ({
    id: group.id,
    label: group.label,
    accent: practiceClass[group.id],
  })),
  { id: 'contact', label: 'Contact', accent: 'ink' },
];
---

<SectionDotNav items={dotNav} />

<main class="landing">
  <section class="hero" id="intro" aria-labelledby="hero-title">
    <div class="section-inner">
      <h1 id="hero-title">
        {isWorkPage ? 'Selected work across systems, bodies, and worlds.' : homepage.hero.h1}
      </h1>
      <p class="bridge">{homepage.hero.bridge}</p>
      <div class="cta-row" role="group" aria-label="Hero actions">
        {homepage.hero.ctas.map((cta) => (
          <a class:list={['button', cta.label === 'Contact' && 'secondary']} href={cta.href}>
            {cta.label}
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="section compass-section" id="topics" aria-labelledby="practices-label">
    <div class="section-inner">
      <div class="section-label" id="practices-label">{homepage.topicsLabel}</div>
      <div class="compass-grid">
        {homepage.practices.map((practice) => (
          <a
            class:list={['compass-panel', practiceClass[practice.id]]}
            href={`#${practice.id}`}
            aria-label={`${practice.title}: ${practice.lead}`}
          >
            <div class="compass-copy">
              <h2>{practice.title}</h2>
              <p class="lead">{practice.lead}</p>
              <p class="meaning">{practice.meaning}</p>
            </div>
            <span class="compass-projects">{practice.related}</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="section work-section" id="work" aria-label="Selected work">
    <div class="section-inner">
      <div class="work-groups">
        {homepage.workGroups.map((group) => (
          <section class:list={['work-group', practiceClass[group.id]]} id={group.id} aria-labelledby={`${group.id}-label`}>
            <div class:list={['group-head', practiceClass[group.id]]}>
              <span class="section-label group-label" id={`${group.id}-label`}>{group.label}</span>
              <h2 class="thesis-claim">{group.thesis.claim}</h2>
              <p class="thesis-body">{group.thesis.body}</p>
            </div>
            <div class="project-grid">
              {group.projects.map((project) => (
                <article class="project-card" id={project.id}>
                  <a
                    class="project-media"
                    href={project.href}
                    style={materialStyle(project.material)}
                    tabindex="-1"
                    aria-hidden="true"
                  ></a>
                  <div class="project-copy">
                    <h3><a href={project.href}>{project.name}</a></h3>
                    <p>{project.introduction}</p>
                  </div>
                </article>
              ))}
            </div>
          </section>
        ))}
      </div>
    </div>
  </section>

  <section class="contact-section" id="contact" aria-labelledby="contact-title">
    <div class="section-inner">
      <p class="invite" id="contact-title">{homepage.contact.invitation}</p>
      <div class="cta-row" role="group" aria-label="Contact actions">
        {homepage.contact.ctas.map((cta) => (
          <a class:list={['button', cta.label === 'View Work' && 'quiet']} href={cta.href}>
            {cta.label}
          </a>
        ))}
      </div>
    </div>
  </section>
</main>

<style>
  .landing {
    /* board geometry is authored at 1440 with 72px margins; zoom scales the
       whole composition ~120% and --max compensates so margins stay 72px */
    --max: 1144px;
    zoom: 1.2;
    color: var(--ink);
    background: var(--paper);
    font-family: var(--sans);
  }

  .landing a:focus-visible {
    outline: 2px solid #111111;
    outline-offset: 4px;
  }

  .section-inner {
    width: min(var(--max), 100%);
    margin: 0 auto;
    padding: 0 32px;
  }

  h1,
  h2,
  h3,
  p {
    margin: 0;
  }

  /* Hero */
  .hero {
    padding: 50px 0 70px;
  }

  .hero h1 {
    max-width: 720px;
    font-family: var(--serif);
    font-size: 46px;
    font-weight: 700;
    letter-spacing: 0;
    line-height: 1.18;
  }

  .bridge {
    max-width: 650px;
    margin-top: 27px;
    color: var(--ink-soft);
    font-size: 18px;
    line-height: 1.6;
  }

  .cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-top: 14px;
  }

  .button {
    min-height: 48px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1.5px solid var(--ink);
    padding: 0 26px;
    background: var(--ink);
    color: var(--paper);
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.06em;
    line-height: 1.2;
    text-decoration: none;
    text-transform: uppercase;
  }

  .button:hover {
    border-color: var(--worlds);
    background: var(--worlds);
    color: var(--paper);
  }

  .button.secondary,
  .button.quiet {
    background: transparent;
    color: var(--ink);
  }

  .button.quiet {
    border-color: var(--rule);
  }

  .button.secondary:hover,
  .button.quiet:hover {
    border-color: var(--worlds);
    background: transparent;
    color: var(--worlds);
  }

  /* Section scaffolding */
  .section {
    padding: 70px 0;
  }

  .section-label {
    display: block;
    color: var(--ink-soft);
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.12em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  /* S1 - practice compass */
  .compass-section {
    padding-top: 70px;
  }

  .compass-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 20px;
    margin-top: 34px;
  }

  .compass-panel {
    min-height: 380px;
    display: grid;
    align-content: space-between;
    gap: 28px;
    border-left: 4px solid currentColor;
    padding: 40px;
    background: var(--surface);
    text-decoration: none;
    transition: background 160ms ease;
  }

  .compass-panel:hover,
  .compass-panel:focus-visible {
    background: var(--paper-3);
  }

  .systems { color: var(--systems); }
  .embodied { color: var(--embodied); }
  .worlds { color: var(--worlds); }

  .compass-copy {
    display: grid;
    grid-template-rows: minmax(54px, auto) minmax(108px, auto) auto;
  }

  .compass-panel h2 {
    color: var(--ink);
    font-family: var(--serif);
    font-size: 28px;
    font-weight: 700;
    letter-spacing: 0;
    line-height: 1.2;
  }

  .compass-panel .lead {
    color: var(--ink-soft);
    font-size: 18px;
    line-height: 1.24;
  }

  .compass-panel .meaning {
    color: var(--ink-soft);
    font-size: 13px;
    line-height: 1.45;
  }

  .compass-projects {
    color: currentColor;
    font-size: 13px;
    font-weight: 500;
    line-height: 1.2;
  }

  /* S2 - selected work index */
  .work-section {
    padding-top: 70px;
    padding-bottom: 60px;
  }

  .work-groups {
    display: grid;
    gap: 72px;
  }

  .work-group {
    position: relative;
    color: var(--systems);
  }

  .work-group.embodied {
    color: var(--embodied);
  }

  .work-group.worlds {
    color: var(--worlds);
  }

  .work-group + .work-group {
    padding-top: 72px;
  }

  .work-group + .work-group::before,
  .work-group + .work-group::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
  }

  .work-group + .work-group::before {
    right: 0;
    border-top: 1px solid var(--rule);
  }

  .work-group + .work-group::after {
    width: 128px;
    border-top: 4px solid currentColor;
  }

  .group-head {
    display: grid;
    gap: 14px;
  }

  .group-label {
    color: currentColor;
  }

  .thesis-claim {
    color: var(--thesis-claim);
    font-size: 24px;
    font-weight: 600;
    letter-spacing: 0;
    line-height: 1.25;
  }

  .thesis-body {
    max-width: 680px;
    color: var(--thesis-body);
    font-size: 16px;
    line-height: 1.45;
  }

  .group-head.systems .thesis-body {
    max-width: 850px;
  }

  .group-head.embodied .thesis-body {
    max-width: 720px;
  }

  .group-head.worlds .thesis-body {
    max-width: 820px;
  }

  .project-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 36px;
    margin-top: 24px;
  }

  .project-card {
    min-height: 450px;
    display: grid;
    grid-template-rows: 200px auto;
    background: var(--surface);
  }

  .project-media {
    display: block;
    background: linear-gradient(180deg, var(--m-from), var(--m-to));
  }

  .project-copy {
    display: grid;
    align-content: start;
    gap: 14px;
    padding: 28px;
  }

  .project-copy h3 {
    font-family: var(--serif);
    font-size: 21px;
    font-weight: 700;
    letter-spacing: 0;
    line-height: 1.2;
  }

  .project-copy h3 a {
    color: var(--ink);
    text-decoration: none;
  }

  .project-copy h3 a:hover {
    color: var(--worlds);
  }

  .project-copy p {
    color: var(--ink);
    font-size: 15px;
    line-height: 1.5;
  }

  /* S4 - contact */
  .contact-section {
    padding: 60px 0 100px;
  }

  .invite {
    max-width: 604px;
    color: var(--ink);
    font-family: var(--serif);
    font-size: 22px;
    font-style: italic;
    line-height: 1.35;
  }

  .contact-section .cta-row {
    margin-top: 72px;
  }

  @media (max-width: 1296px) {
    .compass-grid {
      grid-template-columns: 1fr;
    }

    .compass-copy {
      grid-template-rows: auto;
      gap: 18px;
    }

    .compass-panel {
      min-height: 0;
    }

    .hero h1 {
      font-size: 38px;
    }
  }

  @media (max-width: 1080px) {
    .project-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 912px) {
    .section-inner {
      padding: 0 20px;
    }

    .hero {
      padding: 44px 0 64px;
    }

    .hero h1 {
      font-size: 34px;
      line-height: 1.16;
    }

    .bridge {
      font-size: 17px;
    }

    .section {
      padding: 56px 0;
    }

    .compass-panel {
      padding: 28px;
    }

    .work-groups {
      gap: 72px;
    }

    .work-group + .work-group {
      padding-top: 56px;
    }

    .invite {
      font-size: 20px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .landing *,
    .landing *::before,
    .landing *::after {
      transition: none !important;
    }
  }
</style>
