---
import Base from '../layouts/Base.astro';
import HomepageLanding from '../components/HomepageLanding.astro';
import { site } from '../data/site';
---

<Base
  title={`${site.name} — ${site.homeTitle}`}
  description={site.description}
  wide
>
  <HomepageLanding />
</Base>
